import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const FilterScreen = ({ navigation }) => {
  return (
    <View>
      <Text>It will be Filter</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export { FilterScreen };
